import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {height: 60, justifyContent: 'center', backgroundColor: 'skyblue'},
  flexContainer: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
